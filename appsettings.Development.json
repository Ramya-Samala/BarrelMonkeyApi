{
  // When running locally (ASPNETCORE_ENVIRONMENT=Development), override
  // the API key with something easy to type — or leave it blank to skip auth.
  "Auth": {
    "ApiKey": ""
  },

  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
